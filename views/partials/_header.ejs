<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
      <div class="form-row">
        <div class="col d-flex align-items-center">
          <% if (locals.user_id !== undefined) {%> 
            <div>Logged in as: <%= locals.email %></div>
            <form method="POST" action="/logout?_method=PUT">
              <button class="btn btn-outline-dark">Logout</button>
            </form>
          <% } else { %>
            <a href="/login"><button class="btn btn-outline-dark">Login</button></a>
            <a href="/register"><button class="btn btn-outline-dark">Register</button></a>
          <% } %> 
        </div>
      </div>
  </nav>
</header>